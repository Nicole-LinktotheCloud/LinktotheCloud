name: Deploy to Hostinger

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Set up SSH key
        env:
          SSH_KEY: ${{ secrets.SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p "${{ secrets.SSH_PORT }}" "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts

      - name: Deploy via rsync
        run: |
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/deploy_key -p ${{ secrets.SSH_PORT }} -o StrictHostKeyChecking=yes" \
            dist/ \
            "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.DEPLOY_PATH }}/"

# ---------------------------------------------------------------------------
# FTPS FALLBACK — enable this job and disable build-and-deploy above if
# your Hostinger plan does not include SSH access (requires Premium or higher).
# Create the FTP_* secrets listed in README.md before enabling.
# ---------------------------------------------------------------------------
#
#  ftps-deploy:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Set up pnpm
#        uses: pnpm/action-setup@v4
#        with:
#          run_install: false
#
#      - name: Set up Node
#        uses: actions/setup-node@v4
#        with:
#          node-version-file: .nvmrc
#          cache: pnpm
#
#      - name: Install dependencies
#        run: pnpm install --frozen-lockfile
#
#      - name: Build
#        run: pnpm build
#
#      - name: Deploy via FTPS
#        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
#        with:
#          server: ${{ secrets.FTP_HOST }}
#          username: ${{ secrets.FTP_USER }}
#          password: ${{ secrets.FTP_PASSWORD }}
#          local-dir: dist/
#          server-dir: ${{ secrets.FTP_PATH }}/
#          protocol: ftps
